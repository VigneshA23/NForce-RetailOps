import { Clock } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

function History() {
  return <PlaceholderPage title="History" icon={Clock} />;
}

export default History;
