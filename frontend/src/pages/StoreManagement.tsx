import { Store } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

function StoreManagement() {
  return <PlaceholderPage title="Store Management" icon={Store} />;
}

export default StoreManagement;
